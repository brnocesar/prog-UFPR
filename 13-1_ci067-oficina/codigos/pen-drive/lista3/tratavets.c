#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define N 5

void recebe_user(int vetor[]){
	for(i=0; i<N; i++){
		printf("digite o elemento %d: ", i+1);
		scanf("%d", &vetor[i]);
	}
}

int main(){
	int primeiro[N], segundo[N], terceiro[N], quarto[N], quinto[N], *sexto[N];
	
	
}
